ANTHROPIC_API_KEY=sk-ant-...

# Required only for "audit by address" — fetches verified source from the chain.
# One Etherscan V2 key covers every supported chain. https://etherscan.io/apis
ETHERSCAN_API_KEY=
